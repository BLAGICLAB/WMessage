# Batch Spec: MI-08

## 目的

C5-MI-08（rules.rs:99 + :67，含 1 security）：
1. CSV 表头 `contains` 宽容匹配过松（"子关键字" 命中 "关键字"；多列同 needle 静默取首列）
   → 按列别名集精确匹配 + 重复命中拒绝。
2. archive_dir 无 containment 校验（`../..` 经相对路径拼接逃逸桌面基准目录）
   → resolve_archive_dir 单点拒绝 `..` 组件（覆盖篡改 JSON + CSV 导入 + run 全路径），
   validate_rules 同步加导入期早错。

## 人类可读摘要

- family: input-validation（入口校验缺/过松，fail-closed 收紧）
- 覆盖 findings: 2（C5-MI-08 全簇；批内 2 处独立改动——表头匹配 vs 路径 containment，
  修复设施不同，triage 已标注）
- 预估 diff: 3 files / +80/-8 lines（B 类：find_col 重写 + 新 helper + 新测试块）
- OCR 计划: r1, timeout 1800s, 期望 comments ≤ 3

## 红线

- family 一致性：只含 input-validation；批内 2 处独立改动如实标注
- **绝对路径不动**：ops.rs:73 注释明示"绝对路径原样"是文档化特性，拒绝绝对路径 =
  产品/语义变更 → B 类，攒批报人拍（本批只拒 `..` 组件——无任何文档化用途，纯对抗面）
- 表头别名集 = 官方模版原词 + 裸 needle（启用 / 文件名关键字·关键字 / 动作 /
  归档目录·目录）：官方模版行为不变；其余自定义表头收窄为拒绝（finding 的明确诉求）
- 不改 parse_rules_csv / resolve_archive_dir / validate_rules 签名（错误走既有
  Result 通道）；domain 用 "migration"（与函数内表头错误一致，不沿用 FRDV-01-OCR-1
  挂起的 "csv" 不一致）
- 编译层事实必须核实，不许凭印象

## spec 起草后自查三条（APW-02a 2026-09-23 立）

1. `expected_files` → rules.rs（find_col + validate_rules）+ ops.rs（reject helper +
   resolve_archive_dir 调用）+ migration/mod.rs（测试）。无签名 ripple；run.rs:118
   调用点已核（Err → 跳过 + 记日志，fail-closed 兼容）；commands.rs:77 import 路径
   已走 validate_rules。✓
2. budget → B 类：rules.rs +16/-4；ops.rs +9/-0；mod.rs 测试 +45/-0；
   合计 +70/-4 → budget +80/-8。✓
3. findings fix 字段列 ripple → 见 JSON。✓

自查未过不许发审。补完再审。

## 自主执行规则

spec 被 reviewer 批准后:
- agent 全权执行至 commit,不中途报 status / exit / diff
- 例外只有 stop 条件触发
- 完成时一次报: hash + 批定性 + OCR 轮次 + comments 处置 + follow-up
- 过程证据落 ~/.openclaw/cache/<batch-id>/,不喊人

## Stop 条件(触发即停,报 reviewer)

- compile_failure (无 X 形态退路)
- architecture_blocker (签名/调用点超预算)
- family_heterogeneity
- new_high_different_root (OCR r1 出的 high 非本批根因)
- gate_fail (batch-verify FAIL 且非 spec 声明调整可解)

## 机器可读（脚本读取，勿改格式）

```json
{
  "batch_id": "MI-08",
  "family": "input-validation",
  "expected_files": [
    "src-tauri/src/migration/rules.rs",
    "src-tauri/src/migration/ops.rs",
    "src-tauri/src/migration/mod.rs"
  ],
  "max_lines_added": 80,
  "max_lines_removed": 8,
  "findings": [
    {"id": "C5-MI-08.1", "file": "src-tauri/src/migration/rules.rs", "line": 99, "fix": "find_col contains → 按列别名集精确匹配（启用 / 文件名关键字·关键字 / 动作 / 归档目录·目录，Trim::All 已修边）+ 同 needle 多列命中 → Err(domain:migration)。ripple：migration/mod.rs 新增 2 测试（重复列拒绝 / contains 型自定义表头如 子关键字 拒绝）"},
    {"id": "C5-MI-08.2", "file": "src-tauri/src/migration/ops.rs", "line": 74, "fix": "新增 reject_parent_dir_components helper（拒 Component::ParentDir）；resolve_archive_dir 展开后调用（单点收口：篡改 JSON / CSV 导入 / run 全覆盖，run.rs:118 Err→跳过记日志已兼容）；rules.rs validate_rules move 分支同步调用（导入期早错）。ripple：migration/mod.rs 新增测试（.. 拒绝 / 正常相对路径与绝对路径放行 / validate_rules 拒 ../escape）"}
  ],
  "assertions_min": {
    "src-tauri/src/migration/mod.rs": 128
  },
  "ocr_plan": {
    "rounds": 1,
    "timeout_seconds": 1800,
    "expected_max_comments": 3
  },
  "stop_conditions": [
    "compile_failure",
    "architecture_blocker",
    "family_heterogeneity",
    "new_high_different_root",
    "gate_fail"
  ]
}
```

## 提交信息骨架

```
fix(migration): MI-08 — CSV 表头精确匹配 + archive_dir containment（C5-MI-08 全簇）

【family】input-validation（批内 2 处独立改动：表头匹配 / 路径 containment，设施不同）
【实修 2 条】
- rules.rs:99 find_col：contains 宽容匹配 → 列别名集精确匹配 + 重复命中拒绝
  （"子关键字" 不再命中 "关键字"；归档目录+目录 双列不再静默取首列）
- ops.rs:74 resolve_archive_dir：拒绝 .. 组件（单点收口，run/导入/篡改 JSON 全覆盖）；
  rules.rs validate_rules move 分支同步校验（导入期早错）
【行为变更】自定义 contains 型表头从静默错绑列 → 导入报错；含 .. 的 archive_dir 从
静默逃逸桌面基准 → 导入报错 / run 跳过记日志。绝对路径放行不变（文档化特性，
收窄方向 = B 类待拍）。
【D2】行为断言：4 个别名集精确命中 / 重复列 Err / 子关键字 Err；.. 拒绝 / 正常相对
与绝对路径放行。前置断言：官方模版解析不变（既有测试全绿）。反例断言：若 contains
回归，则"子关键字"用例静默绑错列——新测试挂。
【自测】cargo fmt / cargo check --tests / scripts/test-all.sh 全绿
【OCR】r1
```

## 验证命令

```bash
BATCH_SPEC=docs/batches/MI-08.spec.md python3 scripts/batch-verify.py docs/batches/MI-08.spec.md
```

## 不在本批

- archive_dir 绝对路径收窄 / allow-listed base directory（finding 的 "ideally" 项）：
  "绝对路径原样"是文档化特性（ops.rs:73），收窄 = 产品语义变更 → B 类攒批报人拍
- C5-MI-05a（spawn_blocking 无 abort → CancellationToken 贯穿 run_migration 阶段）：
  签名改 + 取消语义方向（迁了一半的文件怎么办）→ B 类攒批
- C5-MI-05b（sync block_on）：跨域阻塞族待核区，不动
