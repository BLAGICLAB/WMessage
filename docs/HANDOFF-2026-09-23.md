# HANDOFF-2026-09-23

> 状态落文件，不落对话。这本身就是今天验证过的原则。
> 任何"已核清 / 已贴 / 已修"对话记忆不可信。只信 git log / 文件内容 / 脚本输出。

---

## 0. 元信息

- 当前 HEAD: `15e3ef5`（META-1，本地未 push）
- 当前位置: `/Users/renshi/Projects/wmessage`
- Branch: `main`
- 远程同步: `git status` ahead of `origin/main` by 3 commits（META-1 + C5-AP-06 + docs(triage)）
- 今日是否 push: 否（按 09:04 拍板"新 batch 落地后稳一稳"）

---

## 1. 已落地 commits（head -5）

```
15e3ef5 chore(tooling): META-1 — 工具链 batch-verify 升级 + 8 项改动同 commit
0b6ee69 fix(api): C5-AP-06 独立批（error-visible-non-blocking family）
b87a58a docs(triage): PHASE2-TRIAGE §1 family 4 个 + §3.5 异常 2 处置 + handlers follow-up 登记
f93f4a9 fix(db): C5-DB-01a-3 首批补全（error-not-propagated family）
87fd99d chore(triage): PHASE2-TRIAGE.md 收口 + baseline 审计工具入版本控制
eca3622 首批 Phase 2 — error-not-propagated family 2 簇 / 5 条（amend 后不可动）
```

---

## 2. 已落地工具链（META-1）

5 文件已 git add + commit（`15e3ef5`）：

- `scripts/batch-verify.py`（7196 → 266 行；266 是 commit 后扩展版本）：spec-driven gate，含 --quiet / --tests / --json / --repo-root
- `scripts/install-git-hook.sh`：marker-based 所有权检测，幂等（md5 双跑一致）
- `docs/BATCH-SPEC-TEMPLATE.md`：模板 + 自主执行规则段 + stop_conditions（compile_failure / architecture_blocker / family_heterogeneity / new_high_different_root / gate_fail）
- `docs/batches/C5-AP-06.spec.md`：C5-AP-06 spec 实例（已 commit）
- `.githooks/pre-commit`：HOOK_TEMPLATE 生成物，跑 install-git-hook.sh 重新生成

---

## 3. 今日已建 spec（未执行）

- `docs/batches/META-1.spec.md`：已执行（`15e3ef5`）
- `docs/batches/DB-01b-BT-01b.spec.md`：已拍板，**未执行**（下批从这条开始）

`docs/batches/HANDLERS.spec.md` 已撤销（NEW-HANDLERS-FP-1 登记）：3 处 handlers silent-discard finding 实际不在 OCR 原文里（OCR r1 /tmp/ocr-C5-AP-06-r1.json handlers.rs 评论数=0），after_change + notify_change 都返 `()`，无 Err 可 log。

---

## 4. 关键教训（不要重踩）

### 4.1 HANDLERS 批撤销（NEW-HANDLERS-FP-1 登记）

- OCR 报告有"3 条 handlers.rs:342/496/584 silent-discard"——**脑补**，OCR 原文 0 条 handlers.rs
- `TaskStore::notify_change`（api.rs:34）trait 默认体：返 `()`
- `after_change`（util.rs:90）：返 `()`
- 两签名都不返 Result → 无 Err 可 log → fix 路径错
- 教训：**spec 前提必须先核签名，不许凭印象写 fix 字段**

### 4.2 baseline 漂移 6 次

任何数从文件/脚本读，不从对话记忆。如：
- `find ~/.openclaw/cache /tmp -name "*C5*" -name "*.json"` 找 OCR 路径
- `jq '.comments | length'` 找总评论数
- `git show HEAD:path` 找当前代码（不要 `git log` 找历史）

### 4.3 报告层"已贴 ✓ 无内容"

证据贴原文，不写"✓"。hunk/git show/commit message 用内联反引号 + 换行。

### 4.4 过程证据路径

`~/.openclaw/cache/<batch-id>/` 落证据（gate-pre-commit.txt / three-layer.txt / ocr-r1.json / hook-output.txt）。查时去取，不主动报。

---

## 5. 批次执行形态（今日新立）

spec 批准后 agent 全权跑到底——改码 → git add → batch-verify → 三层自测 → OCR r1 → commit。中间不报 status / exit / diff。

**Stop 条件触发才打断**：
- `compile_failure`
- `architecture_blocker`
- `family_heterogeneity`
- `new_high_different_root`（OCR r1 出的 high 非本批根因）
- `gate_fail`（batch-verify FAIL 且非 spec 声明调整可解）

**完成后一次报**：
- hash
- 批定性（family / findings 数 / diff stat）
- OCR 轮次 + comments 数 + 处置
- follow-up

---

## 6. Hook 物理约束

`.githooks/pre-commit` 已挂，`core.hooksPath = /Users/renshi/Projects/wmessage/.githooks`。

src/ 或 src-tauri/ 有 staged 改动时必须：

```bash
BATCH_SPEC=docs/batches/<BATCH_ID>.spec.md git commit -m "..."
```

否则 hook 拒绝（BATCH_SPEC 未设 → exit 1; spec 文件不存在 → exit 1; batch-verify FAIL → exit 1）。

---

## 7. 自主执行的具体步骤（按新形态）

```bash
# 1. git add staged files (per expected_files in spec)
git add <path1> <path2> ...

# 2. gate (--quiet 形态，PASS 输出 commit_allowed: true)
BATCH_SPEC=docs/batches/<BATCH_ID>.spec.md python3 scripts/batch-verify.py docs/batches/<BATCH_ID>.spec.md

# 3. 三层自测（手动跑，不 --tests，因为 hook 拦 gate；agent 跑避免编译错漏过）
cd src-tauri && cargo fmt --check
cd src-tauri && cargo check --tests
bash scripts/test-all.sh

# 4. OCR r1 (前台 1800s)
ocr review --format json --audience agent --exclude 'scripts/**'

# 5. commit (BATCH_SPEC 设好)
BATCH_SPEC=docs/batches/<BATCH_ID>.spec.md git commit -m "<commit message body>"

# 6. evidence 落 ~/.openclaw/cache/<batch-id>/
mkdir -p ~/.openclaw/cache/<batch-id>
cp /tmp/ocr-*.json ~/.openclaw/cache/<batch-id>/ocr-r1.json
# gate-pre-commit.txt / three-layer.txt / hook-output.txt 已由上面步骤 capture
```

---

## 8. 未完成 family 待批（继承候选）

按 09:10 用户拍板"今天还能开一批（优先 failure-recovery-default-value，含数据破坏优先）"：

| Family | Finding 数 | 备注 |
|---------|----------|------|
| **failure-recovery-default-value**（DB-05 部分拆出）| 4 → 待重批 | DB-05 silent mutex poisoning 拆出独立决策（涉 C3-1 约定改不改：X 保持 log-and-continue / Y 修订为 fail-closed），单批待拍 |
| failure-recovery-default-value（剩余）| 2 + 2 + 1 = 5 | DB-01b + BT-01b 在 DB-01b-BT-01b spec（本批待执行） |
| atomicity-partial-write | 2 簇 3 条 | C5-MI-06 + C5-DB-02a |
| error-not-propagated（首批未修完）| 5 簇 ~17 条 | 首批 eca3622 已修 2 簇 5 条 |
| 其他 family | visibility-quote / input-validation / race-TOCTOU 等 | |

---

## 9. follow-up 累积（PHASE2-TRIAGE.md §3.5 已登记）

- **PHASE2-TRIAGE-NEW-HANDLERS-FP-1**：HANDLERS spec 撤销，OCR 报告脑补 finding（FP 登记完整流程）
- **PHASE2-TRIAGE-NEW-META-2**：`set -euo pipefail` after cd
- **PHASE2-TRIAGE-NEW-META-3**：batch-verify.py 缺失兜底
- **PHASE2-TRIAGE-NEW-META-4**：python3 hardcoded
- **PHASE2-TRIAGE-NEW-META-5**：grep -qE 全扫性能
- **PHASE2-TRIAGE-NEW-META-6**：hook 用 --fast 不带 --tests，编译错漏过（spec 加 `manual_gate_full_tests: true` 字段，hook 校验存在——下批起生效）
- **PHASE2-TRIAGE-NEW-META-7**：报告层脑补 findings（不核 OCR 原文直接报为事实）——与 NEW-HANDLERS-FP-1 同源
- **明日 triage spot check（已登记，明日做）**：随机抽 5–10 条 finding 核对 OCR 原文

---

## 10. baseline（271 ± 2 unique non-vendor high / 162 unique path）

脚本核验：

```bash
python3 scripts/triage-baseline-audit.py
# 断言 1 通过：145 + 126 = 271（脚本核验，非记忆）
```

---

## 11. 新窗口起点指令

直接执行 `docs/batches/DB-01b-BT-01b.spec.md`：

```bash
cd /Users/renshi/Projects/wmessage
# 1. git status（核 working tree 干净）
git status
# 2. read spec
cat docs/batches/DB-01b-BT-01b.spec.md
# 3. apply 5 changes per findings array:
#    - DB-01b.1: workspace.rs:129 `?` 传播
#    - DB-01b.2: workspace.rs:254 `?` 传播
#    - BT-01b.1: tools.rs:147 active_tasks 签名改 + body `?`
#    - BT-01b.2a: tools.rs:160 tool_list_tasks 接 Err 转 ToolResult::ok(fail)
#    - BT-01b.2b: tools.rs:516 tool_complete_task 接 Err
#    - BT-01b.2c: tools.rs:531 tool_delete_task 接 Err
# 4. git add 2 files (workspace.rs + tools.rs)
# 5. batch-verify gate (PASS 才进 step 6)
# 6. 三层自测（手动跑，避免 NEW-META-6 漏过）
# 7. OCR r1（前台 1800s）
# 8. commit
# 9. 一次报
```

不要重读这份 HANDOFF——它只作为起点的状态参考。任何"以为记得"的判断都先核 git log / 文件内容 / 脚本输出。