# Batch Spec: DB-04

## 目的

C5-DB-04（input-validation，2 条）：bot_sessions.rs:115 rename 空标题静默落库 → fail-closed
拒绝；tasks.rs:485 tasks_import 用户路径零校验（任意文件读入 + 无大小上限）→ 复用
check_export_path 扩展名校验 + 64 MiB 大小上限。

## 人类可读摘要

- family: input-validation（入口参数缺校验，fail-closed 收紧）
- 覆盖 findings: 2（C5-DB-04 全簇：bot_sessions.rs:115 + tasks.rs:485，含 1 security）
- 预估 diff: 3 files / +55/-8 lines（B 类：新增校验块 + 新测试块）
- OCR 计划: r1, timeout 1800s, 期望 comments ≤ 3

## 红线

- family 一致性：本批只含 input-validation，不混入其他 family
- 不改两命令签名（`bot_session_rename` / `tasks_import` 参数与返回类型不动，前端零改动）
- **不动同文件 NEW-2 登记站**（bot_sessions.rs:100/:112 静默 into_inner——已登记下轮评估）
- 不修同语句 medium（bot_sessions.rs:115 "execute 影响行数被丢 → rename 不存在 id 返 Ok"：
  返 NotFound 是 API 语义变更，medium 非本 Phase 范围，留 follow-up）
- 编译层事实必须核实，不许凭印象

## spec 起草后自查三条（APW-02a 2026-09-23 立）

1. `expected_files` 是否覆盖全部写入路径 → bot_sessions.rs（校验块）+ tasks.rs（import 校验
   + check_export_path reason 中性化）+ db/mod.rs（check_export_path 单测 3 个，:313 测试模块）。
   无签名 ripple；前端 ChatPanel.tsx:655 调用点已核（whitespace-only 标题被 reject 时走既有
   `.catch(silent)`，标题保持"新对话"，无破坏）。✓
2. budget 是 A 类还是 B 类？→ B 类（新增块）：bot_sessions +9/-2；tasks +17/-1；
   db/mod 测试 +18/-0；合计 +44/-3 → budget +55/-8。✓
3. findings 逐条 fix 字段是否显式列出 ripple 的文件 + 行号？→ 见 JSON；唯一 ripple =
   check_export_path reason 文案中性化（影响 tasks_export 报错文本，行为变更标注进
   commit message）。✓

自查未过不许发审。补完再审。

## 自主执行规则

spec 被 reviewer 批准后:
- agent 全权执行至 commit,不中途报 status / exit / diff
- 例外只有 stop 条件触发
- 完成时一次报: hash + 批定性 + OCR 轮次 + comments 处置 + follow-up
- 过程证据落 ~/.openclaw/cache/<batch-id>/,不喊人

## Stop 条件(触发即停,报 reviewer)

- compile_failure (无 X 形态退路)
- architecture_blocker (签名/调用点超预算)
- family_heterogeneity
- new_high_different_root (OCR r1 出的 high 非本批根因)
- gate_fail (batch-verify FAIL 且非 spec 声明调整可解)

## 机器可读（脚本读取，勿改格式）

```json
{
  "batch_id": "DB-04",
  "family": "input-validation",
  "expected_files": [
    "src-tauri/src/db/bot_sessions.rs",
    "src-tauri/src/db/tasks.rs",
    "src-tauri/src/db/mod.rs"
  ],
  "max_lines_added": 55,
  "max_lines_removed": 8,
  "findings": [
    {"id": "C5-DB-04.1", "file": "src-tauri/src/db/bot_sessions.rs", "line": 115, "fix": "bot_session_rename 顶部 trim 后空标题 → CommandError::InvalidArgument{field:title}（fail-fast，取锁/开库前）。无 ripple：签名不变，前端 ChatPanel.tsx:655 既有 .catch(silent) 兼容"},
    {"id": "C5-DB-04.2", "file": "src-tauri/src/db/tasks.rs", "line": 485, "fix": "tasks_import 顶部复用 check_export_path（.json 扩展名）+ spawn_blocking 内 metadata 大小上限 64MiB（MAX_IMPORT_BYTES const）→ InvalidArgument。ripple：check_export_path reason 文案中性化（\"导出路径必须是 .json 文件\" → \"必须是 .json 文件\"），tasks_export 报错文本随之变（行为变更标注）；check_export_path 单测 3 个落 src-tauri/src/db/mod.rs:313 测试模块"}
  ],
  "assertions_min": {
    "src-tauri/src/db/mod.rs": 146
  },
  "ocr_plan": {
    "rounds": 1,
    "timeout_seconds": 1800,
    "expected_max_comments": 3
  },
  "stop_conditions": [
    "compile_failure",
    "architecture_blocker",
    "family_heterogeneity",
    "new_high_different_root",
    "gate_fail"
  ]
}
```

## 提交信息骨架

```
fix(db): DB-04 — 入口 input-validation fail-closed（rename 空标题 + import 路径零校验）

【family】input-validation（入口参数缺校验 → 静默落库 / 任意文件读入）
【实修 2 条】
- bot_sessions.rs:110 bot_session_rename：trim 后空标题 → InvalidArgument{field:title}
  （取锁/开库前 fail-fast；旧行为 = 空白标题静默 UPDATE 落库）
- tasks.rs:507 tasks_import：复用 check_export_path（.json 扩展名）+ 64MiB 大小上限
  （MAX_IMPORT_BYTES）；旧行为 = 任意路径任意大小读入内存解析
- ripple：check_export_path reason 中性化（"导出路径必须是 .json 文件" → "必须是 .json
  文件"），tasks_export 报错文本随之变；check_export_path 3 单测落 db/mod.rs
【行为变更】rename 空/全空白标题从静默成功 → Err；import 非 .json / >64MiB 从读入尝试
→ Err；export 校验报错文案变。
【D2】行为断言：三处 Err 路径带 field/reason；check_export_path 3 测试锁 .json 接受 /
  非 json 拒绝 / 无扩展名拒绝。前置断言：合法调用行为不变（正常标题 / 合法小文件 /
  export 校验仍生效）。反例断言：若 import 不再校验扩展名，则 tasks_import 顶部无
  check_export_path 调用点（grep 可见）——保护存在性可机械核查。
【自测】cargo fmt / cargo check --tests / scripts/test-all.sh 全绿
【OCR】r1
```

## 验证命令

```bash
BATCH_SPEC=docs/batches/DB-04.spec.md python3 scripts/batch-verify.py docs/batches/DB-04.spec.md
```

## 不在本批

- bot_sessions.rs:115 同语句 medium（execute 影响行数被丢 → rename 不存在 id 返 Ok）：
  返 NotFound 是 API 语义变更，medium 非 Phase 2 high 范围，留 follow-up
- bot_sessions.rs:100/:112 静默 into_inner：PHASE2-TRIAGE-NEW-2 已登记，下轮评估
- tasks_import canonicalize + 限定导入目录（finding 的 (c) 项，原文标 "consider"）：
  限定目录是产品 policy（用户从哪选文件），B 类候选，攒批报人拍
