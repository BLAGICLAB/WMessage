# 人工冒烟测试验收清单（2026-09-26）

> 依据：`docs/HANDOFF-2026-09-26-v3.md`（收官状态）+ `docs/OCR-FIX-PLAN-2026-09-21.md`
> （1145 条审计的修复计划）+ 各批账本（PHASE2/3-TRIAGE、AUTO-LOG）。
> 覆盖范围：Phase 1–6 全部用户可验行为 + 本日两批新落地（DEC-1 / MP-01）。
> 每条标注来源批号，可回查账本核对预期依据。
>
> **用法**：逐条操作 → 对照「预期」→ 通过打 `[x]`，失败留 `[ ]` 并在 §10 记录现象与截图。
> ⭐ = 快速冒烟子集（约 15 分钟，赶时间只测带星项）。
> 标记约定：`[x]` 通过 ｜ `[ ]` 未通过/未测 ｜ 条件不满足写 `➖ 跳过` 并注一句原因。

## 0. 前置条件

- [ ] 测试版本：`main` 分支 HEAD ≥ `05ca8f2`（含 DEC-1 + MP-01）
- [ ] 启动方式：dev 实例 `npm run tauri dev`（已验证可启动，进程内嵌新代码）
- [ ] bot 已配置 API Key + 至少 1 个模型（设置页，Key 存系统钥匙串）
- [ ] 数据目录：`~/Library/Application Support/com.renshi.wmessage/`（dev 与正式包共用，测试会产生真实数据，注意备份或用测试任务）

---

## 1. 挂件（WidgetApp）— DEC-1 / MP-01 / WA 簇

- [ ] ⭐ **高度下界 400**：拖挂件边框把高度往小缩，能一路缩到约 400px 才停，不再被弹回 800 `[来源: DEC-1 / C4-v2]`
- [ ] ⭐ **默认视觉不变**：展开挂件，默认高度仍为 560，与升级前观感一致 `[来源: DEC-1 / C4-v2]`
- [ ] **高度上界 900 仍生效**：往大拖不超过约 900px `[来源: DEC-1]`
- [ ] **尺寸记忆**：缩到 ~500 → 收起 → 重启应用 → 恢复的是 ~500（不会被拒收回退到默认）`[来源: DEC-1；修复前 560 存量尺寸会被 loadSize 判非法]`
- [ ] ⭐ **🧠 弹模型列表**：点挂件聊天区「🧠 当前模型」标签，弹出模型列表，当前模型高亮，再点收起 `[来源: MP-01]`
- [ ] ⭐ **选中即切换 + 双窗同步**：点列表中另一个模型 → 标签立即刷新；**主窗口**聊天区的 🧠 也同步变成同一模型 `[来源: MP-01 / bot-config-changed 广播]`
- [ ] **设置页一致**：打开设置页，active 模型已随第 6 条切换（后端真实落盘，非仅前端显示）`[来源: MP-01]`
- [ ] **贴边/收起/展开回归**：拖挂件贴右缘/左缘/顶部收成触发条，悬停/点击展开正常 `[来源: C2a-Q1 永久 wontfix 决策回归（拖拽是产品特性）]`
- [ ] **宽度回归**：横向拖挂件边框，宽度仍可在 400–800 调整 `[来源: WA 簇回归，本批未动宽度]`

## 2. 聊天面板（主窗口 + 挂件共用）— P3-BUG-2 / MP-01 回归

- [ ] ⭐ **会话三件套**：新建对话 → 发一条消息 → 切到另一对话再切回 → 删除会话，全程正常 `[来源: P3-BUG-2 B56/B57/B60 回归]`
- [ ] **多字节标题**：建一个含 emoji（如 🎯🤖）标题的会话，标题渲染完整不闪退、截断处无乱码 `[来源: P3-BUG-2 B56/57：字节切片 → chars().take(8)]`
- [ ] **重命名反馈**：重命名会话立即生效；对已删除的会话重命名应报错而非假成功（如可构造）`[来源: P3-BUG-2 B60]`
- [ ] **🧠 空列表指引**（➖ 可跳过，需先清空模型列表）：模型列表为空时点 🧠，显示「模型列表为空，去设置页添加」而非空白 `[来源: MP-01]`
- [ ] **切换模型失败可见**（➖ 可跳过，需停用钥匙串等手段构造）：切换失败时聊天区有错误提示，不静默 `[来源: MP-01 / handleCommandError 非 silent]`

## 3. 机器人与 Python 沙箱 — BOT-B / DEC-1 S20

- [ ] ⭐ **bot 开关高度联动**：设置页开/关机器人 → 挂件面板高度在「任务区」与「完整面板」间正确切换 `[来源: Phase 2 回归 + DEC-1 关联]`
- [ ] ⭐ **S20 限额留痕**：让 bot 跑一次会执行 Python 的功能（如文档修订/含 py 工具的技能），然后：
  ```bash
  grep 'rlimit' ~/Library/Application\ Support/com.renshi.wmessage/bot.log | tail -3
  ```
  本机（macOS）预期：出现 `rlimit_off | RLIMIT_AS: 限额未生效(...)`（内核本就拒 AS）且 **CPU 限额照设**；不再有「静默无记录」。`[来源: DEC-1 / S20 探测降级]`
- [ ] **py 任务可用**：同上一条的场景，py 任务本身正常出结果（沙箱环境下不再被硬失败打断）`[来源: S20 历史回退登记的回归确认]`
- [ ] **允许目录保存后 key 不失效**：设置页新增一条允许目录并保存 → 回聊天区正常对话（API key 仍有效，未被误剥）`[来源: BOT-B #10 add_allowed_dir 剥 key 前置读回验证]`
- [ ] **字体四档同步**：设置页切字号（small/standard/large/xlarge）→ 主窗口与挂件同步生效 `[来源: 既有广播机制回归，MP-01 复用同一通道]`

## 4. 数据与迁移 — DB-B / MI-B1/B2/B3 / APW 系列

- [ ] ⭐ **启动读库**：应用启动后任务列表/看板/归档数据完整，无报错弹窗 `[来源: DB-B #3 copy_legacy_db fail-closed 回归]`
- [ ] **任务增删改查**：新建 → 编辑 → 完成 → 归档 → 回收站还原 → 彻底删除，全链路正常 `[来源: DB 域批量修回归]`
- [ ] **迁移规则损坏可见**（➖ 条件项，需手工损坏规则文件）：migration 规则文件读取失败时报错区分「首装无文件」与「文件坏了」，不再静默用空规则 `[来源: MI-B1 #1]`
- [ ] **迁移可取消**（➖ 条件项，需有可归档数据）：跑一次归档迁移 → 中途点取消 → 安全停止、已完成部分不回滚、报告标 cancelled `[来源: MI-B2 #2]`
- [ ] **迁移界面不卡 UI**：打开迁移面板 + 大列表滚动，主线程无明显卡顿 `[来源: MI-B3 sync→async spawn_blocking]`

## 5. 前端主应用 — FE-B / C4 系列

- [ ] ⭐ **看板拖拽**：看板/工作区卡片拖拽排序正常，多窗口（主窗+挂件）状态一致 `[来源: FE-B #18 tasks-updated 广播回归]`
- [ ] **月度视图日期**：月度/周期视图的年份月份显示正确（无 month-as-year 错位）`[来源: C4-3 format.ts monthly 已修]`
- [ ] **全局快捷键**：⌘W 唤起/隐藏主窗口、⌘N 快速新建、⌘T 切主题 `[来源: 回归]`
- [ ] **升级孤儿防护**（➖ 开发自查项，需构造 legacy 数据）：legacy 迁移失败时标记错误并跳过 SEED，不产生永久 orphan `[来源: FE-B #19]`

## 6. 文件打开与安全面 — C1/C2 系列 / P3-SEC-1

- [ ] ⭐ **打开文件走白名单**：聊天/挂件打开工作区文件正常；对白名单外路径（如系统目录）请求被拒 `[来源: C2b-1 路径校验加固]`
- [ ] **workspace 链接白名单**（➖ 条件项）：写入 kind=url/file/folder 之外类型的链接被 fail-closed 拒绝 `[来源: C2b-2]`
- [ ] **API key 不落明文**：`cat ~/Library/Application\ Support/com.renshi.wmessage/bot-config.json`，确认无 API key 明文字段（key 在系统钥匙串）`[来源: BOT-B / P3-SEC-1 S0]`
- [ ] **Transfer-Encoding 防护**（➖ 条件项，需 API 服务开启）：
  ```bash
  curl -X POST http://127.0.0.1:<port>/api/... -H 'Transfer-Encoding: chunked' -d '{}'
  ```
  预期被拒（即拒而非解析）`[来源: P3-SEC-1 S2]`
- [ ] **无 token 401**（➖ 条件项，需 API 服务开启）：不带 Authorization 请求 API → 401 `[来源: 回归]`

## 7. 进化系统 — C3 / EV-B / EVNB

- [ ] **演进面板**：打开 evolution 面板，proposal 列表正常显示；promote/reject/rollback 按钮可用 `[来源: Phase 2 evolution 域回归]`
- [ ] **坏行 fail-closed**（➖ 条件项，需手工损坏 shadow jsonl 首行）：面板/读取对「首个非空行损坏」报错而不是静默空列表；中间坏行跳过并留痕 `[来源: EV-B #17 read_jsonl 收敛]`

## 8. 文档工具与杂项 — py 域 / C1

- [ ] **docx 修订**（➖ 条件项，需模型+key+docx 工具）：对 .docx 跑一次修订 → 输出带 track changes 的文档正常 `[来源: py/document 回归 + C5-AP-02 token 写保护间接覆盖]`
- [ ] **复制文件**：向工作区复制文件（含重名覆盖场景）正常 `[来源: C1 copy_file 三修回归]`
- [ ] **任务执行对话**：对任务执行一次 bot 操作 → 「查看执行对话」跳转正常 `[来源: bot 域回归]`

## 9. 门禁（开发侧自查，非人工点击项）

- [ ] `bash scripts/test-all.sh` 全绿（cargo nextest + pytest tests-audit strict 零 xfail + vitest）
- [ ] `git rev-parse origin/main HEAD` 两值相等（验收基线在远端可追溯）

## 10. 验收结论（测完填写）

| 项 | 数量 |
|---|---|
| 通过 `[x]` | |
| 失败 `[ ]` | |
| 跳过 `➖` | |

- 测试环境：macOS ___ / 构建hash ___ / 日期 ___
- 测试人：___

**发现的问题（现象 + 截图路径 + 复现步骤）：**

1.
2.
3.

> 失败项处置规则（照 HANDOFF v3 §6）：真实复现的问题提 issue 走批循环（spec → 改码 →
> batch-verify → commit → docs 收口）；登记过的 wontfix/defer 项不算失败。
