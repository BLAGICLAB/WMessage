# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri / Rust
src-tauri/target
src-tauri/gen/schemas

# Windows green-package dist artifacts (don't commit binaries / dist zip)
MicrosoftEdgeWebview2Setup.exe
wmessage-win-x64-*.zip
README.txt

# Vitest coverage output
coverage/
*.lcov

# .NET 构建产物（修订工具 src-tauri/dotnet/）
src-tauri/dotnet/**/bin
src-tauri/dotnet/**/obj

# 绿色便携包产物（本地打包，勿入库）— 同时匹配目录和 zip
wmessage-portable-*

# TypeScript 增量编译缓存（tsconfig.node.json composite: true 触发）
tsconfig.*.tsbuildinfo

# vite.config.ts 的 tsc 编译产物（vite.config.d.ts + 编译后的 vite.config.js）
vite.config.d.ts
vite.config.js

# pytest 缓存
.pytest_cache/

# ───────── API key / 凭据 / 用户数据（防御性，勿入库） ─────────
# dotenv（含多环境）
.env
.env.*
# wmessage 主配置（key 已走 macOS Keychain，但文件本身含 schema，不入库）
bot-config.json
# SQLite 数据库（任务/记忆/凭据缓存）
wmessage*.db
wmessage*.db-journal
# Linux 上 keyring 不可用的明文 fallback（0600 权限，本机不生成，仅 CI/无人值守场景兜底）
bot-api-key.txt
bot-tavily-key.txt
bot-brave-key.txt

# Windows 绿色便携包解包目录（本地验收产物）
wmessage-portable/


# Sprint 切片阶段产物（worktree 临时目录，每次 run 重生成；patch 已在 commit 历史里）
.audit-*/

# 自进化系统 worktree 临时目录（每次 run 重生成；commit 41a68e6 + docs/evolution/phase-1.md 已留底）
.evolution-*/

# Bug Hunt session worktree（每次 run 重生成；commit 留底在 docs/bug-hunt-*/）
.bug-hunt-*/

# Comment Hygiene session worktree（每次 run 重生成；commit 留底在 docs/comment-hygiene-*/）
.comment-hygiene-*/

# AI agent working dirs (local scratch, not for git history)
.opt-botchat-*
.t3-*
.bug-hunt-*
.comment-hygiene-*
.evolution-*
.audit-*
.api-opt-*

# 多 skill 审计产物（9/17 + 未来）
.comment-consistency-*/

# R6 A 旧合成数据（synthetic 隔离目录，eval / eval_set / 副本 jsonl）
evolution.synthetic/

# 老板诊断脚本（不进项目代码）
health-check.sh
