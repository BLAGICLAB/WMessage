# BT-06 spec — C5-BT-06 判定：OCR false positive ×2 + FP 诱因（陈旧注释）清除

## 判定（先证据后结论）

C5-BT-06 两条 high（bot_model_loop.rs:1079 工具行 / :984 hallucination_guard 行）称
「truncate_for_log 只限长度不剥换行，模型可控字符串可伪造日志行，fix isn't applied」。

**事实核**：

- `bot/config/audit.rs:56` `escape_for_log`：`| `→`|  `、裸 `|`→`||`、`\n`→`\\n`、
  `\r`→`\\r`，再截断。
- `bot/config/audit.rs:74` `truncate_for_log` = **escape_for_log 的向后兼容别名**
  （注释原文：「行为即 escape_for_log（同族日志伪造问题一并修复）」）。
- 引入时间：dcbf167（2026-09-14 21:44），**早于** OCR 全扫（2026-09-21）——
  OCR 读到的代码已是修复后形态。
- bot_model_loop.rs 两站点均经 `crate::bot::truncate_for_log` → 已转义，注入面不存在。

**结论：2 条均 false positive**（前提「truncate_for_log 不剥换行」与实现不符），
与 C5-BT-02（FP×3）同处理：记录 + 不改行为。

**FP 诱因**：:1081 行内注释「工具名是模型给的字符串，直插可伪造日志行」是修复前
遗留，描述的行为与现状相反，直接误导 OCR（finding 原文复述了该注释）。注释 hygiene
不修 = 下次扫描复报。

## 目标 findings（2 条，判定 FP；1 处注释修正）

1. **bot_model_loop.rs:1079**（high → FP）：工具行 name/args/result 均经
   truncate_for_log（=escape_for_log），`\n\r|` 已转义。
2. **bot_model_loop.rs:984**（high → FP）：final_text 同经 truncate_for_log。
3. **:1081 陈旧注释**（FP 诱因）：改述现状（truncate_for_log = escape_for_log 别名，
   已转义，不可伪造日志行）。唯一代码改动 = 注释 1 行，零行为变更。

## spec 起草后自查三条

1. `expected_files` → `src-tauri/src/bot_model_loop.rs`（单文件，注释 1 行）。✓
2. budget → A 类：+1/-1 → budget +2/-2。✓
3. findings fix 字段列 ripple → 无 ripple（注释行，无签名/调用点影响）。✓

## 自主执行规则

spec 被 reviewer 批准后:
- agent 全权执行至 commit,不中途报 status / exit / diff
- 例外只有 stop 条件触发
- 完成时一次报: hash + 批定性 + OCR 轮次 + comments 处置 + follow-up
- 过程证据落 ~/.openclaw/cache/<batch-id>/,不主动读回

## Stop 条件(触发即停,报 reviewer)

- compile_failure (无 X 形态退路)
- architecture_blocker (签名/调用点超预算)
- family_heterogeneity
- new_high_different_root (OCR r1 出的 high 非本批根因)
- gate_fail (batch-verify FAIL 且非 spec 声明调整可解)

## 机器可读（脚本读取，勿改格式）

```json
{
  "batch_id": "BT-06",
  "family": "ocr-false-positive",
  "expected_files": [
    "src-tauri/src/bot_model_loop.rs"
  ],
  "max_lines_added": 2,
  "max_lines_removed": 2,
  "findings": [
    {"id": "C5-BT-06.1", "file": "src-tauri/src/bot_model_loop.rs", "line": 1079, "fix": "FP 判定（truncate_for_log=escape_for_log 别名，已转义）；:1081 陈旧注释改述现状。ripple：无"},
    {"id": "C5-BT-06.2", "file": "src-tauri/src/bot_model_loop.rs", "line": 984, "fix": "FP 判定（同 .1，final_text 同路径已转义）；无代码改动。ripple：无"}
  ],
  "assertions_min": {
    "src-tauri/src/bot_model_loop.rs": 124
  },
  "ocr_plan": {
    "rounds": 1,
    "timeout_seconds": 1800,
    "expected_max_comments": 2
  }
}
```
