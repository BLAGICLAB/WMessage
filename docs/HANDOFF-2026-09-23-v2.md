# HANDOFF — 2026-09-23 v2

> 切换至 Kimi Code 的交接文档。v1 已失传，本 v2 由小九在 21:06 从会话状态重建。
> 来源：今日 17 个 turn 的对话历史 + git log / 7 docs commit + APW-02b OCR evidence。

## 窗口状态（截至 21:05）

- HEAD: `bd206d0fe5b7ce5ac592054376cd564423c5f7f1`
- origin/main: `71a35bf72e7280bec197a342d947a048c0cd1dfc`（未变）
- 8 commit 待 push：bd206d0 / 67e1b04 / 33dcc53 / 027fb4d / 8b0c55d / 382ded4 / c216078 +（若 c216078 计入则 7）

## 当前分支本地 8 commit ahead of origin/main

```
bd206d0 docs(triage): PROC-5 type 3 评估（n=2 触发后；不出建议，出选项 + 事实）
67e1b04 docs(triage): PROC-5 type 3（rate-limit）n=2 —— 触发条件达成
33dcc53 fix(bot): BT-01a — bot_slash flag 删除错误丢弃收口（error-not-propagated family）
027fb4d docs(triage): BT-01a spec budget 实测校正 6/3 → 7/1（numstat +7/-1）
8b0c55d docs(triage): BT-01a spec v3（实入 1 条）+ C5-BT-01a-2 退批登记
382ded4 docs(triage): BT-01a spec v2（实入 2 条）+ C5-BT-01a-1 退批登记
c216078 docs(triage): APW-02b OCR r1 disposition（audit 链修正 + 5 条逐条处置 + 2-5 挂起索引）
71a35bf docs(triage): APW-02b 记录修正 4 件 + SOP §5 budget 调整两类（已 push）
```

## 未完成动作（按优先级）

1. **PROC-5-TYPE3-EVAL.md 起草** —— **已在 bd206d0 提交**（4761 bytes）
   - 路径：`docs/PROC-5-TYPE3-EVAL.md`
   - 内容：现象（两次 429 元数据）/ 可能原因 / 调用侧 3 选项（A backoff / B 频率 / C 换端点）/ 未决问题
   - **reviewer 待拍**：选 A / B / C（或暂不动作）

2. **push 8 commit** —— 等待 #1 拍板后
   - 约束：禁 --force / --force-with-lease
   - 流程：push 前贴 `git log origin/main..HEAD --oneline` + `git rev-parse origin/main`；push 后贴两值相等
   - **注意**：pre-push hook 跑 test-fast.sh（实际是 full test-all）；已观察 3 次 flaky：
     - APW-02b 全量：1 failed（exit_cleanup 端口 4763 冲突）
     - BT-01a 全量：1 failed（exit_cleanup 端口 4763 冲突）
     - 21:02 push attempt：1 failed（task_chat_exec BotDisabled）
     - 同族不同子（端口 vs BotDisabled），3 次内不开 PROC，2 次时再评估

3. **flaky 索引登记** —— 等 #2 完成 / 下次 flaky 出现
   - 暂未落盘（无专门 docs 索引），可在 `PHASE2-TRIAGE.md` §3.5 后加 PEND-FLAKY 段
   - 当前样本：port-4763 ×2 + BotDisabled ×1

## 批执行形态（小九确立）

1. **spec 起草** → 写 `docs/batches/<BATCH_ID>.spec.md`
2. **spec 审** → reviewer 拍（一轮可能多 v：v1 → v2 → v3）
3. **执行**：改码 → numstat 校正 → gate → 三层（fmt + check + test-all）→ OCR r1 → commit
   - docs commit 与 code commit **必须分开**（hook `src-tauri` staged 时要求 `BATCH_SPEC` env；docs-only 不需要）
4. **完成一次报**：hash + 批定性 + OCR 轮次 + comments 处置 + follow-up

## 工具链

- **OCR**：本地 CLI（`ocr review --format json --audience agent --exclude 'scripts/**'`）
  - 已知不稳：5 类失败（PROC-5）：
    - 类型 1：file_read tool-bug（start_line > end_line）— 1fcc418
    - 类型 2：timeout-class hang — APW-02a
    - 类型 3：rate-limit 429 — APW-02b run B + BT-01a r1（**n=2 触发**）
    - 累积规则：n≥2 触发单批评估（已评估，见 `docs/PROC-5-TYPE3-EVAL.md`）
  - **路径撞车教训**：同输出名 `/tmp/ocr-<BATCH>-r1.json` 不能复用（run B 覆盖 run A）；改为 `ocr-<BATCH>-<STAMP>.json`

- **pre-commit hook**：`.githooks/pre-commit`
  - 若 staged 含 `src/` 或 `src-tauri/`，要求 `BATCH_SPEC=docs/batches/<ID>.spec.md` env
  - docs-only commit：先 `git restore --staged src-*`，再 commit
  - docs/batches/*.spec.md 可放心 staged（不在 hook 拦截路径）

- **batch-verify.py**：脚本读取 spec JSON 做 gate
  - 检查 file_set / line_budget / findings_files_in_diff / worktree_clean
  - 上限是 spec 写的 max_lines_added/removed；实测超过 → 校正一次

- **scripts/test-fast.sh**（实为 full）：pre-push hook 调用
  - 已观察 3 次 flaky（端口 4763 ×2 + BotDisabled ×1）
  - 同族 3 次内不开 PROC

## 教训清单（今日累积，已入 PHASE2-TRIAGE.md + 新 SOP §5）

1. **动作前必核 HEAD**（amend 默认改 HEAD，今日栽 3 次：8f99c46 误改 docs / 958358e 描述不准 / 第二次未重核 HEAD）
   - SOP §5「动作前必核 HEAD」已入 PHASE2-TRIAGE.md

2. **budget 调整分两类**（SOP §5）：
   - 估算错（实现前后差 ±20% 内）：允许一次
   - scope 变更：先 stop 报 reviewer；不许直接调 budget 过 gate
   - 例：APW-02a 调两次 90→121→144（SOP 破例，已在 code commit message 显式声明）

3. **OCR 输出命名按 run_id**，不硬编码 `-r1`（路径撞车教训）

4. **多 OCR run 时先穷尽核对**所有 .json 输出（防 APW-02b 误把"complete / 5 findings" run A 误报为"failed 429" run B）

5. **patch OCR 文档时修正 commit message（不 amend 已 push）**；不写空头"tag typo"修正（你 21:00 自我纠正：基于转述的断言 → 错误判断 → 写空修正 → 污染 audit 链）

6. **模型申报**首条响应必写 `Agent: 小九 | Model: <name> | Provider: <name>`

7. **family 越界即退批**：C5-BT-01a-1（commands.rs migrate → error-visible-non-blocking）、C5-BT-01a-2（keyring.rs → error-visible-non-blocking）、#4（skill_finish → String 非 Result）、#5（fn → Vec + unwrap_or_default → failure-recovery-default-value）

## family 现况

| family | 已落批 | 在飞 / 待审 | 退批登记 |
|---|---|---|---|
| error-not-propagated | APW-02b（copy_legacy_db 3 文件 swap）/ BT-01a（bot_slash flag delete） | — | C5-BT-01a-1 / C5-BT-01a-2 / #4 / #5 |
| failure-recovery-default-value | — | — | #5（候选）/ BT-01b 已 batch |
| error-visible-non-blocking | C5-AP-06（独立 1 条） | — | C5-BT-01a-1 / C5-BT-01a-2 待并入 |
| atomicity-partial-write | APW-01（legacy DB 事务）/ APW-02a（copy_dir_recursive）/ APW-02b（copy_legacy_db） | — | C5-DB-03 TOCTOU 待核 |
| poisoned-silent-recovery | 已溶解 | — | — |
| error-not-propagated | error-not-propagated 首批约 17 条未完成 | — | 排队 |

## Follow-up 索引

- **APW-02b OCR 5 条**（在 `PHASE2-TRIAGE.md` §3.5）：
  - critical paths.rs:97 → **已在 497e4c1 修复**（Phase 2a/2b tmp.exists() 守卫）
  - medium paths.rs:79（Ok(0) sentinel）→ 挂起 APW-02b-OCR-2
  - low mod.rs:456（恒真断言）→ 挂起 APW-02b-OCR-3
  - low mod.rs:419（测试未触达 tmp-* 清理）→ 挂起 APW-02b-OCR-4
  - low paths.rs:90（4 组 cleanup 近似重复）→ 挂起 APW-02b-OCR-5

- **C5-BT-01a 退批**（在 `PHASE2-TRIAGE.md` §3.5）：
  - C5-BT-01a-1（commands.rs:30/:31 migrate → (b) log-and-continue → error-visible-non-blocking）
  - C5-BT-01a-2（keyring.rs:272-276 → (d) 原顺序 + 双 Error 分流 → error-visible-non-blocking）

- **PROC-5 type 3 评估**：`docs/PROC-5-TYPE3-EVAL.md`（bd206d0）
  - 选项 A：backoff 参数（OCR 工具暴露才有效）
  - 选项 B：调用频率上限（影响 batch discipline）
  - 选项 C：换端点 / 换 model（绕 quota pool，但不解决根本）
  - 未决：failure_details 为空是工具默认？跨批频率？provider session quota？

- **§2 簇清单 version / last-checked 标注**：下次 docs commit 带（BT-01a 发现 10 条中 4 条 path/line 漂移，doc 未到 spec level）

- **C5-EV-3b-A**：退 triage 重做（family 混 + 模式不统一 + 行号漂移），不在本轮

## 当前工作树状态

```
$ git status --short
M docs/HANDOFF-2026-09-23-v2.md (本文件)
?? docs/HANDOFF-2026-09-23.md (v1，已失传？需核)
?? docs/batches/APW-01.spec.md
?? docs/batches/APW-02a.spec.md
?? docs/batches/C5-AP-06.spec.md
?? docs/batches/DB-01b-BT-01b.spec.md
?? docs/batches/FRB-01-FAILCLOSED.spec.md
?? docs/batches/HANDLERS.spec.md
?? docs/batches/META-1.spec.md
?? docs/batches/BT-01a.spec.md (新)
?? memory/ (workspace 子目录)
```

## 交接 commit 完成 → 新窗口从「未完成动作」第 1 件开始

