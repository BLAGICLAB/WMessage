# Batch Spec: P6T-1

## 目的

Phase 6-T 收口批：11 条 TOCTOU/权限策略自决裁决落 `docs/PHASE6-T-TOCTOU-POLICY.md`（10 接受/defer/wontfix + 1 修）+ 唯一代码项 C1b-7（bot_fs walk / 白名单读失败静默 → eprintln 可见化，语义不变）。

## 人类可读摘要

- family: toctou-policy-closure
- 覆盖: 11 条策略裁决 + C1b-7 代码（3 处静默点）
- 预估 diff: 2 文件 / +55/-6
- OCR 计划: r1（安全相邻域，跑）, timeout 1800s, 期望 comments ≤ 3

## 逐条处置（用户授权自决 2026-09-26）

**代码 ① src-tauri/src/bot_fs.rs:335/:338/:155（C1b-7）**：walk 的 read_dir 失败 / entry Err / 白名单 canonicalize JoinError 三处静默。**修法**：各补 eprintln（含路径/错误），失败语义不变（跳过继续 / 回空白名单 fail-closed 维持）。
**裁决 ②-⑪**：见 docs/PHASE6-T-TOCTOU-POLICY.md 裁决表（C1b-1..6 接受/wontfix/维持 + C2a-1/2/3 + C2c-v1 接受/defer/触发器）。

## 测试

- 既有 walk_skips_symlinks 等测试回归担保（eprintln 不改行为）

## 红线

- 权限模型/capabilities 不动；fail-closed 方向不动；恢复语义不变
- 新注释/日志串不引用审计批次号

## spec 起草后自查三条

1. expected_files：docs/PHASE6-T-TOCTOU-POLICY.md + src-tauri/src/bot_fs.rs = 2
2. budget：A 类（3 处 eprintln + 注释 ≈ +16/-3）+ policy doc 新文件 ≈ +45 → 合计 ≈ +60/-3，上限 +100/-10；新文件 45 行走 max_new_files_lines 60
3. fix 字段：C1b-7 文件内闭环，无调用链变化

## 自主执行规则 / Stop 条件

用户授权全自动（2026-09-26）；五条 stop 触发即停。

## 机器可读（脚本读取，勿改格式）

```json
{
  "batch_id": "P6T-1",
  "family": "toctou-policy-closure",
  "expected_files": [
    "docs/PHASE6-T-TOCTOU-POLICY.md",
    "src-tauri/src/bot_fs.rs"
  ],
  "max_lines_added": 100,
  "max_lines_removed": 10,
  "max_new_files_lines": 60,
  "findings": [
    {"id": "C1b-7", "file": "src-tauri/src/bot_fs.rs", "line": 335, "fix": "walk read_dir 失败 / entry Err / 白名单 JoinError 三处静默补 eprintln 可见化（跳过/空白名单语义不变）；policy doc 承载其余 10 条裁决。ripple：无"}
  ],
  "assertions_min": {
    "src-tauri/src/bot_fs.rs": 0
  },
  "ocr_plan": {
    "rounds": 1,
    "timeout_seconds": 1800,
    "expected_max_comments": 3
  },
  "stop_conditions": ["compile_failure", "architecture_blocker", "family_heterogeneity", "new_high_different_root", "gate_fail"]
}
```

## 提交信息骨架

```
fix(bot): P6T-1 — toctou-policy-closure（Phase 6-T 收口）

【family】TOCTOU/权限 11 条策略自决 + C1b-7 可见化
【实修】bot_fs walk/read_dir/JoinError 三处静默补 eprintln（语义不变）
【策略】10 条接受/defer/wontfix（PHASE6-T-TOCTOU-POLICY.md 裁决表，威胁模型锚定本地单用户桌面）
【自测】cargo fmt/check + tsc + test-all 全绿
【OCR】r1：<N> comments <处置>
【基线】D2: files=2(+A/-R) asserts=0→0 tests=…
```

## 验证命令

```bash
BATCH_SPEC=docs/batches/P6T-1.spec.md python3 scripts/batch-verify.py docs/batches/P6T-1.spec.md
```
