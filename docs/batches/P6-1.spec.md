# Batch Spec: P6-1

## 目的

Phase 6 门禁恢复：6 条 xfail 断言对齐现实（全转正）+ resolve_entry 收紧（同名就近 → 尾段唯一匹配，歧义响亮失败）+ test-all strict xfail 门禁 + test-fast collect-only 扩 4 文件 + HOOK-AUDIT-01 结案（always_allow 测试现通过，矛盾消解）+ 文档树裸条目补全路径（7 处）。

## 人类可读摘要

- family: tests-audit-gate-restore
- 覆盖: 6 xfail 转正 + 门禁加固 ×3 + 调查结案 + 文档树 8 处路径补全
- 预估 diff: 6 文件 / +75/-35
- OCR 计划: 跳过（测试/门禁批，用户指令）

## 逐条处置（用户授权自决 2026-09-26）

1. audit_pre_step_pre_execute.py：BOT glob 补 bot/config 子树（BotConfig/View 迁移后锚点失联——bypass_llm ×2 转正）；start_skill 断言对齐 start_skill_with_audit 形态；run_pre_execute / skill_on_step / tool.return 三条锚定 execute_tool_impl（薄 wrapper 内化后调入点在 impl）。
2. audit_module_map.py resolve_entry：多同名不再「就近取第一」，尾段唯一匹配才通过，歧义/零匹配响亮失败——暴露文档树 8 处裸条目（16 个 mod.rs 歧义），rust-bot-architecture.md 树图与 §6.4 补全 db//api_handlers//bot/config/ 前缀。
3. test-all.sh：pytest 步骤加 strict 检查（xfailed/xpassed > 0 → exit 1，Phase 6 零 xfail 政策）。
4. test-fast.sh：collect-only 从 1 文件扩 4 文件（语法自检全覆盖）。
5. HOOK-AUDIT-01.md：§5 结案——always_allow 测试实测通过，§1 陈述 3 前提被后续修复推翻，矛盾消解。

## 测试

- pytest 4 文件 38 passed / 1 skipped / 0 xfailed（本批自证）
- test-all 全量含新 strict 门禁跑通

## 红线

- 不改 hook 语义（只扩 collect 文件清单与加 xfail 检查）；文档树只补路径不改结构描述

## spec 起草后自查三条

1. expected_files 6（全路径已列）
2. budget +80/-40（断言重写与 glob/strict 增量）
3. fix 字段：全部测试/脚本/文档文件，无生产 Rust 代码

## 自主执行规则 / Stop 条件

用户授权全自动（2026-09-26）；五条 stop 触发即停。

## 机器可读（脚本读取，勿改格式）

```json
{
  "batch_id": "P6-1",
  "family": "tests-audit-gate-restore",
  "expected_files": [
    "tests-audit/audit_pre_step_pre_execute.py",
    "tests-audit/audit_module_map.py",
    "scripts/test-all.sh",
    "scripts/test-fast.sh",
    "docs/rust-bot-architecture.md",
    "docs/HOOK-AUDIT-01.md"
  ],
  "max_lines_added": 90,
  "max_lines_removed": 45,
  "findings": [
    {"id": "P6-xfail", "file": "tests-audit/audit_pre_step_pre_execute.py", "line": 110, "fix": "6 条 xfail 断言对齐现实转正：BOT glob 补 bot/config 子树（bypass_llm ×2）/ start_skill_with_audit 形态 / 三条锚定 execute_tool_impl"},
    {"id": "P6-resolve", "file": "tests-audit/audit_module_map.py", "line": 55, "fix": "resolve_entry 收紧：尾段唯一匹配才通过，歧义响亮失败；文档树 8 处裸条目补全路径"},
    {"id": "P6-strict", "file": "scripts/test-all.sh", "line": 15, "fix": "pytest 步骤加 strict 检查（xfailed/xpassed > 0 → exit 1）"},
    {"id": "P6-collect", "file": "scripts/test-fast.sh", "line": 154, "fix": "collect-only 扩 4 文件"},
    {"id": "HOOK-AUDIT-01", "file": "docs/HOOK-AUDIT-01.md", "line": 0, "fix": "§5 结案：always_allow 测试实测通过，矛盾消解"}
  ],
  "assertions_min": {
    "tests-audit/audit_pre_step_pre_execute.py": 0,
    "tests-audit/audit_module_map.py": 0
  },
  "ocr_plan": {
    "rounds": 0,
    "timeout_seconds": 0,
    "expected_max_comments": 0
  },
  "stop_conditions": ["compile_failure", "architecture_blocker", "family_heterogeneity", "new_high_different_root", "gate_fail"]
}
```

## 验证命令

```bash
BATCH_SPEC=docs/batches/P6-1.spec.md python3 scripts/batch-verify.py docs/batches/P6-1.spec.md
```
