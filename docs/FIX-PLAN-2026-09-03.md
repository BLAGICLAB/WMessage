# WMessage 审计未修排期项 · 修复计划（2026-09-03）

> 依据：`docs/AUDIT-SUMMARY-2026-08-28.md` 的「未修排期项」，已对当前代码逐项核查
> （行号为核查时点的当前代码）。已修项取掉，其余分「必修 / 非必要」两档。

## 已修，从排期取掉

| 项 | 状态 | 证据 |
|---|---|---|
| #5 recurring 补跑 2h 窗口 | ✅ 已修 | `bot_scheduler.rs:183` CATCHUP_WINDOW + `classify_due` 三分支 + 边界单测（`:601-634`） |
| #6 绿色包 .NET self-contained | ✅ 产物已达成 | `wmessage-portable-2026-09-02/dotnet/` 含完整运行时；残留尾巴=打包自动化脚本，降为非必要 |
| #10d SchedGuard/ExecGuard 并发漏洞 | ✅ 原漏洞已堵 | 调度路径 `bot_chat.rs:838` 取同一 ExecGuard；「统一为一套守卫」纯架构洁癖，不排 |
| #9 弱断言（部分） | 🔸 仅 skill_e2e.rs:205 已改善 | 其余未修，保留在排期 |
| #10c 审计 append 失败（部分） | 🔸 只修一半 | `audit.rs:144-158` 已返回 bool + 测试；`bot.rs:596` 主路径仍全静默，保留 |

## 第一梯队：必修（有真实用户/数据影响）

### T1-1 整行覆盖 lost-update（原排期 #1，最重要架构项）
- 现状：`src-tauri/src/db.rs:944-962` 19 列全量 `INSERT … ON CONFLICT DO UPDATE SET 全字段=excluded.*` + `WHERE … excluded.updated_at >= tasks.updated_at`；所有写者先刷新时间戳 → 守卫对「读旧快照→整行写回」失效。
- 修复方向：**写前重读比对**（比字段级合并便宜）：写入前重读现行，比对调用方快照基线，冲突则拒写/报错。字段级合并留作后续选项。
- 预估 1–2 天，需回归测试（模拟两写者交错）。

### T1-2 测试盲区合并立项（原排期 #2 + #3）
已修的 P0-1/P0-5 密集区恰在无覆盖区，回归无保护。
- **run_model_loop**：先把 HTTP 层抽成可注入 base_url/client 的纯 async 函数（`bot_model_loop.rs:510-518` 目前函数体内 `bot_get_config`/`read_api_key` 依赖 AppHandle），再对 mock LLM server 补集成测试（`tests/mock_llm.rs` 现成）。
- **run_skill_scheduler 真 e2e**：用现成 `fixtures/minimax-ppt`（已在 `skill_e2e.rs:33` 引用）+ mock LLM server，驱动调度器本体，覆盖确认/审计/持久化/回滚窗口/Done 收尾。
- 预估合计约 1 天，先重构 #3 再写 #2。

### T1-3 bot_stop / bot_confirm_response 静默失败（原排期 #10b）
- 现状：`bot_slash.rs:148`、`:326` 返回 `()`；锁失败（`:163`）、`tx.send` 失败（`:341`）全吞，连 eprintln 都没有。
- 修复：改返回 `Result<(), String>`，失败可见。约半小时。

### T1-4 收尾 bot.rs 的 audit_log 静默（原排期 #10c 残留）
- 现状：`bot.rs:596-599` 仍是 `if let Ok … { let _ = writeln! }` 全静默，bot.log 主路径丢日志零痕迹。
- 修复：对齐 `audit.rs:144-158` 的做法，失败带 path eprintln。约 10 分钟。

### T1-5 死命令 bind_file / db_merge 下线（原排期 #10a）
- 现状：`lib.rs:436`/`:440` 仍注册，前端零调用（`src/` 仅 `TodoCard.tsx:132` 调 `bind_files` 复数）。
- 修复：删除注册与命令体，减攻击面。注意确认无其他调用方。约 10 分钟。

### T1-6 LLM base_url 校验（原排期 #7b）
- 现状：`bot.rs:554-573` `bot_set_config` 直接写盘 base_url，全仓无 https 校验。
- 修复：写盘前非 `https://` 打警告日志（localhost 放行），必要时在返回值带 warning。约半小时。

**第一梯队合计约 3 天，T1-1、T1-2 是大头。**

## 第二梯队：非必要，记录排期（成本高或审计已认定取舍可接受）

| 项 | 结论 |
|---|---|
| #4 API slowloris 换 HTTP 栈 | 仅回环面、过不了鉴权；换 axum/actix 独立立项。维持记录 |
| #7a DNS TOCTOU | ✅ 已修（2026-09-03） |
| ↳ #7a 说明 | `bot_web.rs`：`check_public_url` 改为返回校验过的解析地址，`fetch_text` 每跳重定向与 `fetch_jina_reader` 均用 `resolve_to_addrs` 构建钉 IP 的 per-request client（TLS SNI/Host 仍按原域名），reqwest 不再二次 DNS；回归测试 `pinned_client_uses_validated_addrs` 用 .invalid 域名钉回环验证无二次解析且 Host 头不变 |
| #7c .NET dll 哈希/签名校验 | 发版流水线项，非代码修复 |
| #6 尾巴：打包自动化脚本 | ✅ 已修（2026-09-03）：新增 `scripts/publish-docx-dotnet.sh`（npm 入口 `npm run publish:docx-dotnet`），publish win-x64 self-contained 后剔除 .pdb 归位到便携包 `dotnet/`；产物与手工包逐文件核对一致（189 文件，含运行时包版本号） |
| #8 前端盲区测试 | ✅ 已修（2026-09-03） |
| ↳ #8 说明 | 新增 5 个测试文件 43 例：`KanbanBoard.test.tsx` 8 例（spliceMove 加 export 后直测：同列 below 前后/跨列改 column/列空白区插尾/空列/activeId 不存在原样返回/overId 边界/不可变）；`MigrationPanel.test.tsx` 6 例（规则表渲染、删除规则警告开关、confirm 取消/确认门、日志弹窗、导入后刷新）；`ArchivePage.test.tsx` 7 例（archived+!deletedAt 前置过滤、标题/备注搜索大小写不敏感、标签计数与筛选、标签×搜索 AND、归档默认折叠、空态）；`theme.test.ts` 10 例（getSetting 兜底、effectiveTheme 随 matchMedia、三态循环、toggle 对 system 取反、storage 事件同步与退订、subscribeSystem 含旧 WebView addListener 分支）；`MarkdownText.test.tsx` 12 例（http→openUrl 尾随标点剥离、GFM 裸 URL、白名单路径→open_file_path、mailto/锚点/白名单外路径降级 span、`/tmpfoo` 边界、行内代码 URL/Windows 路径/普通代码、围栏代码块）。未补：KanbanBoard 组件级 dnd 拖拽交互（dnd-kit 在 jsdom 无真实手势，纯逻辑已覆盖）；发现并记录未修：无语言标记的单行围栏代码块因 react-markdown 剥尾换行被误判为行内可点代码 |
| #9 弱断言清理（剩余） | ✅ 已修（2026-09-03） |
| ↳ #9 说明 | migration.rs：`archive_dir_year_expansion`/`archive_dir_year_placeholder_in_nested_path`/`archive_dir_year_placeholder_replaced_multiple_times` 改调新抽的生产纯函数 `expand_year_placeholder`（`resolve_archive_dir` 同走它）；`move_remove_persistent_failure_stops_duplicate_copies` 的循环编排残留复刻（消除需把 run_migration_inner 的单文件迁移段抽成可注入函数，超出纯测试改动范围，且各原语 `record_move_remove_failure`/`move_remove_permanently_failed`/`conflict_free_name` 均为生产直调）。db.rs：抽 `bot_session_delete_inner`（`bot_session_delete_is_atomic` 直调 + 新增 trigger 注入失败回滚断言）、`workspace_import_merge`（`workspace_import_merges…` 直调）；`workspace_commands_spawn_blocking_bridge` 去掉 spawn_blocking 桥接复刻，改直调生产 helper upsert/load/delete（桥接薄壳依赖 AppHandle 本不可单测）。llm_integration.rs：删 `reqwest_handles_401_auth_error_response`（只测 reqwest 库行为；覆盖点已被 core_http_401_wrapped_no_retry 真路径覆盖且更强）。lib 489 + llm_integration 27 全绿 |
| #10e sched_last 跑前记崩溃窗口 | 审计定「取舍可接受」，且 #5 的 2h 窗口部分兜底。不修 |
| #10f 孙进程 setsid 逃逸 | 非沙箱已知取舍，Windows Job Object 已覆盖。不修 |
| #10d 守卫统一 | 漏洞已堵，纯架构整洁项。不排 |

## 执行记录

- [x] T1-1 lost-update 写前重读比对
  - `db.rs`：`Task` 加 `expected_updated_at`（serde default + skip_serializing，仅上行、不落库/不下发）；`upsert_tasks` 写前重读现行行比对基线，不一致/行已删 → Err（`CONFLICT_ERR_PREFIX` 前缀供分流），原时间戳守卫保留兜底。RMW 调用方全部串上基线：`bot.rs` 8 个任务工具、`bot_scheduler.rs` 4 处、`exec_steps.rs`/`bot_chat.rs` 各 1 处、`migration.rs` 8 处（已落盘再入 changed 的重武装防自拒）、API `update_task`/`delete_task`（冲突映射 409）、前端 `diffTaskRows`/`App.tsx` 规则落盘。回归：`upsert_expected_baseline_rejects_stale_write`（两写者交错）+ 原弱断言 `upsert_where_guard_prevents_lost_update` 改调生产函数；前端 `storage.test.ts` 4 例。遗留：字段级合并仍是后续选项；冲突对端可见性 = API 409 / 前端 alert / bot 工具失败文案
- [x] T1-2 run_model_loop 重构 + 集成测试 + run_skill_scheduler e2e
  - `bot_model_loop.rs`：`run_model_loop` 拆为薄壳（取配置/Key、构 HTTP 客户端、装配回调）+ `run_model_loop_core`（注入 `LlmHttp`/`ModelLoopDeps`/execute_tool/replan，不依赖 AppHandle）；`bot_skills/scheduler.rs` 同款拆出 `run_skill_scheduler_core`（泛型 Runtime + 注入 execute_tool/persist_outcome），`skill_finish`/`load_skill_meta` 等随之泛型化。测试：`llm_integration.rs` 新增 8 例 core_*（HTTP 401 包装不重试 / 429 退避重试成功 / 500 耗尽 MAX_LLM_ATTEMPTS=2 / 200 流内错误载荷 / 干净 EOF 残缺 tool_calls 截断防线 / 纯文本截断放行 / 工具回路续聊）；`skill_e2e.rs` 新增 4 例 scheduler_e2e_*（真 fixture + MockRuntime 直驱：Done 收尾 Running→Completed + 审计 + 真落库、Paused 确认窗口 AwaitUser、P0-5 回滚窗口 Failed→Running→Failed、僵尸终态入口清理假死回归）；`state.rs` 加 doc(hidden) 集成测试钩子，`mock_llm.rs` 加 RawSse 变体。全量 `cargo test` 绿（lib 488 + 集成 49）
- [x] T1-3 bot_stop / bot_confirm_response 改 Result
  - `bot_slash.rs`：两命令改返回 `Result<(), String>`；注册表置位抽 `flag_session_stopped`（锁中毒返回 Err），确认流抽 `take_confirm`（不存在/已超时 Err）+ `deliver_confirm`（送达失败 Err）；前端 ChatPanel 调用处本有 `.catch(handleCommandError)`，无需改动；新增 3 个回归测试（`t1_3_command_result_tests`）
- [x] T1-4 bot.rs audit_log 失败带 path eprintln
  - `bot.rs`：`audit_log` 写盘抽 `append_bot_log_line`（open/write 失败 eprintln 带 path + 返回 false，对齐 `audit::append_line` P2-15）；新增 2 个回归测试（`t1_4_audit_log_tests`）
- [x] T1-5 死命令 bind_file / db_merge 下线
  - `lib.rs` 摘除注册；`db.rs` 删除 `bind_file`、`db_merge` 命令体及仅 db_merge 使用的 `load_external`（三处过时注释同步）；前端零调用已确认（`bind_files` 复数形保留）；新增源码锁测试（`t1_5_dead_command_tests`）
- [x] T1-6 base_url https 警告
  - `bot.rs`：`bot_set_config` 写盘前经 `base_url_is_safe` 判定，非 https 且非回环（localhost/127.x/[::1] 放行）打 eprintln 警告，不拒写；新增判定回归测试（`t1_6_base_url_tests`）
- [x] 第二梯队 #6 尾巴：绿色包 .NET self-contained 打包自动化
  - 新增 `scripts/publish-docx-dotnet.sh`：`dotnet publish -c Release -r win-x64 --self-contained` 后整目录拷贝 publish 产物（剔除 `*.pdb`，对齐 2026-09-02 手工包核对结论）到 `wmessage-portable-<日期>/dotnet/`（已 gitignore），幂等重建 + 关键文件校验；package.json 加入口 `publish:docx-dotnet`。验证：macOS cross publish 成功，输出与手工包文件清单 189/189 一致（含 mscordaccore 版本号 8.0.2526.11203），重复运行幂等
- [x] 第二梯队 #9 弱断言清理
  - migration.rs 抽 `expand_year_placeholder` 纯函数（`resolve_archive_dir` 改走它），3 处 year 展开测试（`archive_dir_year_expansion`、`…_in_nested_path`、`…_replaced_multiple_times`）改调生产函数；db.rs 抽 `bot_session_delete_inner`（命令事务段）与 `workspace_import_merge`（导入合并段），`bot_session_delete_is_atomic`（另补 trigger 注入失败的整体回滚断言）与 `workspace_import_merges…` 改直调生产函数，`workspace_commands_spawn_blocking_bridge` 去掉桥接复刻改直调 upsert/load/delete helper；llm_integration.rs 删 `reqwest_handles_401_auth_error_response`（core_http_401_wrapped_no_retry 已覆盖且更强）。残留 1 处：`move_remove_persistent_failure_stops_duplicate_copies` 的循环编排仍是复刻（原语均生产直调；彻底消除需把 run_migration_inner 单文件迁移段抽成可注入函数，超出本次纯测试范围）。验证：lib 489 + llm_integration 27 全绿

## 收口验证（2026-09-03）

- `cargo test` 全目标：lib 488 + llm_integration 28 + mock_llm 8 + skill_e2e 13，全绿
- `vitest`：139 全绿；`tsc -b` 零错（顺手修了 vite.config.ts 预存的失效 `@ts-expect-error`）
- pytest 门禁：23 过 / 2 skip / 0 FAIL（修了 `test_bot_log_path_uses_portable_dir` 的环境性假红：dev 实例只起停无会话时 bot.log 仅含生命周期行，改为显式 skip）

## 收口验证（2026-09-03，第二梯队）

- `cargo test` 全目标：lib 489 + llm_integration 27 + mock_llm 8 + skill_e2e 13，全绿
- `vitest` 182 全绿（+43）；`tsc -b` 零错；pytest 门禁 23 过 / 2 skip / 0 FAIL
- 本轮后第二梯队仅剩：#4（换 HTTP 栈，独立立项；中间选项=fork tiny_http 加读超时）、#7c（发版流水线）、#10e/#10f/#10d（已定不修）
- 新发现已记录未修：MarkdownText 把无语言标记的单行围栏代码块误判为行内代码（块内单行 URL 可变可点链接），修法=用 node.position 判块级
