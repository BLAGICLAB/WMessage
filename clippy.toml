# Clippy 配置（提交门禁只 **warn** 不 deny；**禁止为过 lint 拆函数**）
#
# too_many_lines：阈值从默认 100 收到 200——只招呼「真的该拆」的巨型函数，
# 避免把本就内聚的长函数（工具实现、SSE 主循环）逼成散装小函数。
# 现状摸底见 DEVLOG（记录超长函数清单与行数）；要不要进一步收紧/deny 由清单决定。
too-many-lines-threshold = 200
