# Chunk 3: Python 沙箱

## 上下文
- **仓库**：`/Users/renshi/Projects/wmessage/`
- **已有审计**（请勿重复）：4 份原始审计 + `docs/kimi-audit/kimi-chunk-1-result.md`（API 8 条）+ `docs/kimi-audit/kimi-chunk-2-result.md`（DB 10 条）
- **任务主线**：老板现在遇到不少 bug，找出架构层面的根因（不要小修小补）
- **严格只读**，不改代码

## 任务
审查 wmessage 的 Python 子进程执行层（LLM 调 `run_python` 工具的底层）一个文件，找**架构层 / 沙箱边界 / 资源生命周期 / 错误隔离**的潜在 bug 根因。

## 范围（必读）
- `src-tauri/src/bot_py.rs` (37 KB)

## 重点关注
1. **执行限制**：超时机制（CPython 还是会话级？超时时如何 kill？是否能 kill 残余子进程？）；内存限制（是否有？）；CPU 限制？
2. **资源清理**：临时文件（输入脚本 / 输出文件 / 中间产物）是否清理？脚本运行结束后 stderr/stdout 句柄是否关闭？子进程 zombie？
3. **错误隔离**：Python 脚本崩溃 / 段错误 / 死循环 / OOM 是否会让 Tauri 整个挂掉？进程间通信（stdin/stdout pipe）断开后 Rust 端状态
4. **安全边界**：用户输入的 Python 代码能否逃逸沙箱？能否读任意文件？能否 fork bomb？网络出口控制？环境变量注入？Python 自身是否使用受限模式（`python -I` / `python -S`）？
5. **输出处理**：stdout 截断（大小 / 行数）？二进制输出处理？编码异常（用户脚本 `print` 中文 / emoji）？
6. **状态**：多次运行之间是否共享状态？`__pycache__` / `.pyc` 行为？import 路径？
7. **审计**：是否走 `audit_event!` 钩子？执行耗时 / 脚本长度 / 退出码 / 资源消耗是否记录？
8. **并发**：多个 `run_python` 同时调用如何协调？队列 / 串行 / 互斥？单实例 vs 多进程？

## 跳过（已审过，**不要列**）
- 命令注册（`#[tauri::command]` 签名）—— 上一批命令已审
- 已有 4 份审计 + chunk-1/2 覆盖的项

## 输出格式（≤ 50 行）
1. **风险清单**（按 P0 > P1 > P2 排序）：
   - [P0] [file:line] [一句话描述] [建议]
2. **整体评估**（一句话）
3. **与已有审计差异**：重复 0 条，新发现 N 条

## 约束
- 严格只读，不改代码
- 输出 ≤ 50 行
- 不重复已有 P1/P2 项
- 不要给"小修小补"建议，只给**架构层面**或**潜在 bug 根因**的发现
- 没发现就明说"无新发现"
